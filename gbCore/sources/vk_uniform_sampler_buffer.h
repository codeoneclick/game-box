#pragma once

#if defined(VULKAN_API)

#include "vk_buffer.h"

namespace gb
{
	class vk_uniform_sampler_buffer
	{
	private:

	protected:

	public:

		vk_uniform_sampler_buffer();
		~vk_uniform_sampler_buffer();
	};
};

#endif